export const emailField = (type, id, name, label) => {
  return {
    type,
    id,
    name,
    label,
  };
};

export const createInputField = (type, id, name, label) => {
  return {
    type,
    id,
    name,
    label,
  };
};

// export const passwordField = {
//   type: "password",
//   id: "password",
//   name: "password",
//   label: "Enter Password",
// };
